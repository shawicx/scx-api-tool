/**
 * @description 自由格式检测与 JsonValue 类型定义（已下沉至中立层 @/schema/freeForm）
 * 此处重新导出以保持既有 `from '../generator/freeForm'` / `from './freeForm'` 的导入兼容性；
 * 新代码请直接从 `@/schema` 导入。
 */

export * from '@/schema/freeForm';
